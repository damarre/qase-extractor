import { BaseGetAllParams, UserInfo, UserList } from '../models';
import { AxiosResponse } from 'axios';
import { BaseService } from '.';
export declare class Users extends BaseService {
    getAll({ limit, offset }: BaseGetAllParams): Promise<AxiosResponse<UserList>>;
    get(userId: string | number): Promise<AxiosResponse<UserInfo>>;
    exists(userId: string | number): Promise<boolean>;
}
