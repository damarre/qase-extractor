import { BaseGetAllParams, ProjectCreate, ProjectCreated, ProjectInfo, ProjectList } from '../models';
import { AxiosResponse } from 'axios';
import { BaseService } from '.';
export declare class Projects extends BaseService {
    getAll({ limit, offset }: BaseGetAllParams): Promise<AxiosResponse<ProjectList>>;
    get(code: string): Promise<AxiosResponse<ProjectInfo>>;
    create(data: ProjectCreate): Promise<AxiosResponse<ProjectCreated>>;
    exists(code: string): Promise<boolean>;
}
