import { BaseGetAllParams, CustomFieldInfo, CustomFieldList } from '../models';
import { AxiosResponse } from 'axios';
import { BaseService } from '.';
export declare class CustomFields extends BaseService {
    getAll(code: string, { limit, offset }: BaseGetAllParams): Promise<AxiosResponse<CustomFieldList>>;
    get(code: string, fieldId: string | number): Promise<AxiosResponse<CustomFieldInfo>>;
    exists(code: string, fieldId: string | number): Promise<boolean>;
}
