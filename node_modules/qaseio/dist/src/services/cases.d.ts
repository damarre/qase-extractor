import { BaseGetAllParams, TestCaseFilters, TestCaseInfo, TestCaseList } from '../models';
import { AxiosResponse } from 'axios';
import { BaseService } from '.';
interface CasesGetAllParams extends BaseGetAllParams {
    filters?: TestCaseFilters;
}
export declare class Cases extends BaseService {
    getAll(code: string, { limit, offset, filters }: CasesGetAllParams): Promise<AxiosResponse<TestCaseList>>;
    get(code: string, caseId: string | number): Promise<AxiosResponse<TestCaseInfo>>;
    exists(code: string, caseId: string | number): Promise<boolean>;
    delete(code: string, caseId: string | number): Promise<AxiosResponse<undefined>>;
}
export {};
