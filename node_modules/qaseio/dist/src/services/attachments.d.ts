import { AttachmentCreated, AttachmentInfo, AttachmentList, BaseGetAllParams } from '../models';
import { AxiosResponse } from 'axios';
import { BaseService } from '.';
export declare class Attachments extends BaseService {
    getAll({ limit, offset }: BaseGetAllParams): Promise<AxiosResponse<AttachmentList>>;
    get(hash: string): Promise<AxiosResponse<AttachmentInfo>>;
    create(code: string, ...files: Array<{
        value: string | Blob;
        filename?: string;
    }>): Promise<AxiosResponse<AttachmentCreated>>;
    delete(hash: string): Promise<AxiosResponse<undefined>>;
    exists(code: string): Promise<boolean>;
}
