import { BaseGetAllParams, DefectFilters, DefectInfo, DefectList, DefectUpdated } from '../models';
import { AxiosResponse } from 'axios';
import { BaseService } from '.';
interface DefectsGetAllParams extends BaseGetAllParams {
    filters?: DefectFilters;
}
export declare class Defects extends BaseService {
    getAll(code: string, { limit, offset, filters }: DefectsGetAllParams): Promise<AxiosResponse<DefectList>>;
    get(code: string, defectId: string | number): Promise<AxiosResponse<DefectInfo>>;
    exists(code: string, defectId: string | number): Promise<boolean>;
    resolve(code: string, defectId: string | number): Promise<AxiosResponse<DefectUpdated>>;
    delete(code: string, defectId: string | number): Promise<AxiosResponse<undefined>>;
}
export {};
