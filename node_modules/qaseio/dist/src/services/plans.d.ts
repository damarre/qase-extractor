import { BaseGetAllParams, PlanCreate, PlanCreated, PlanInfo, PlanList, PlanUpdate } from '../models';
import { AxiosResponse } from 'axios';
import { BaseService } from '.';
export declare class Plans extends BaseService {
    getAll(code: string, { limit, offset }: BaseGetAllParams): Promise<AxiosResponse<PlanList>>;
    get(code: string, planId: string | number): Promise<AxiosResponse<PlanInfo>>;
    exists(code: string, planId: string | number): Promise<boolean>;
    create(code: string, data: PlanCreate): Promise<AxiosResponse<PlanCreated>>;
    update(code: string, planId: string | number, data: PlanUpdate): Promise<AxiosResponse<PlanCreated>>;
    delete(code: string, planId: string | number): Promise<AxiosResponse<undefined>>;
}
